{
  "name": "semver",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "semver": "^7.5.4"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
